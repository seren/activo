<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/bodypart_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >

    <ImageButton
        android:id="@+id/fullbody"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/full_body"
        style="?android:attr/borderlessButtonStyle"
        android:layout_above="@+id/corebody"
        android:layout_alignParentStart="true" />

    <ImageButton
        android:id="@+id/corebody"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/core_body"
        style="?android:attr/borderlessButtonStyle"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true" />

    <ImageButton
        android:id="@+id/upperbody"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/upper_body"
        style="?android:attr/borderlessButtonStyle"
        android:layout_alignTop="@+id/fullbody"
        android:layout_alignParentEnd="true"
        android:layout_toEndOf="@+id/fullbody" />

    <ImageButton
        android:id="@+id/lowerbody"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/lower_body"
        style="?android:attr/borderlessButtonStyle"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true" />

    </RelativeLayout>