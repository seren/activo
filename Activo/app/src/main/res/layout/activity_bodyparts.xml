<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/bodypart_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >

    <Button
        android:id="@+id/fullbody"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:drawableTop="@drawable/full_body"
        style="?android:attr/borderlessButtonStyle"
        android:text="@string/fullbody_caption"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_above="@+id/corebody" />

    <Button
        android:id="@+id/corebody"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:drawableTop="@drawable/core_body"
        style="?android:attr/borderlessButtonStyle"
        android:text="@string/corebody_caption"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true" />;

    <Button
        android:id="@+id/upperbody"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:drawableTop="@drawable/upper_body"
        android:text="@string/upperbody_caption"
        style="?android:attr/borderlessButtonStyle"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:layout_alignBottom="@+id/fullbody" />

    <Button
        android:id="@+id/lowerbody"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:drawableTop="@drawable/lower_body"
        style="?android:attr/borderlessButtonStyle"
        android:text="@string/lowerbody_caption"
        android:layout_below="@+id/fullbody"
        android:layout_alignParentEnd="true" />

</RelativeLayout>